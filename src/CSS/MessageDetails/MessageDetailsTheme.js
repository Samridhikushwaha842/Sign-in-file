const MessageDetailsStyles = (theme) => ({
    paper: {
      padding: theme.spacing(3),
    },
    avatar: {
      width: 60,
      height: 60,
    },
    timestamp: {
      color: theme.palette.text.secondary,
    },
    description: {
      marginTop: theme.spacing(2),
    },
  });
  
  export default MessageDetailsStyles;
  